## Installation

To run project locally first run local backend server

```bash
  cd backend
  npm install
  npm run dev
  cd ..
```

Then you can run:

```bash
  cd frontend
  npm install
  npm run dev
```
